{
  "name": "jenkins-monitor",
  "version": "1.0.0",
  "description": "Jenkins CI/CD failure monitor — emails on new build failures",
  "main": "jenkins_monitor.js",
  "dependencies": {
    "nodemailer": "^6.9.13"
  }
}
